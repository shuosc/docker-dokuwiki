version: '3'
services:
  dokuwiki:
    build: .
    image: shuosc/dokuwiki:arm
    ports:
      - 8081:80
    # environment:
    #   - DIR=wiki
    volumes:
      - ./data:/opt/data
      - ./log:/var/log/apache2